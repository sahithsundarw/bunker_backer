# Qualitative examples — `weights/best.pt` (28.0394 dB checkpoint)

Evidence folder. Every panel here was generated from real, on-disk artifacts — the same
checkpoint (`r2_nb8_psnrloss`, SHA256
`37e8571047218a0344c43bcd2246dc559184a75fe301995fea24463dfd341fa7`) that produced the reported
validation metrics and the final-test submission outputs. Nothing here is synthetic or
hand-drawn. Generated by `scripts/make_qualitative_examples.py` (not part of the verified
pipeline or `scripts/verify_all.py` — it uses `matplotlib`, which `inference.py` may never
import at module level per CLAUDE.md STYLE).

## What each panel shows

**Validation panels** (`val_*.png`, 6 files) — four images side by side:
`NoisyLR | Restored | GT | |Restored − GT| error map`, titled with the file's own PSNR/SSIM
computed with the exact pinned settings from `src/metrics.py` / `scripts/evaluate.py`
(`skimage` PSNR/SSIM, `data_range=1.0`, `gaussian_weights=True, sigma=1.5,
use_sample_covariance=False`). Source predictions:
`results/residual_experiments/r2_nb8_psnrloss/preds/final/` (the same disk-verified
predictions directory that produced the reported 28.0394 ± 4.1881 dB / 0.74804 ± 0.15275 /
0.29571 ± 0.16672 numbers over all 400 pairs in `configs/split_val.txt`). GT/NoisyLR are read
from `<data_root>/train/GT` and `<data_root>/train/NoisyLR`.

NoisyLR panels are **display-clipped to [0,1] for visualization only**; the true on-disk range
(shown in the panel subtitle) legitimately exceeds [0,1] per `docs/io_contract.md` and is
never clipped in the actual inference path.

| File | Case | PSNR (dB) | SSIM | Why chosen |
|---|---|---|---|---|
| `val_001323_best_psnr39.50.png` | best in split | 39.50 | 0.9534 | ceiling of what the model achieves |
| `val_000960_strong_psnr36.83.png` | strong | 36.83 | 0.9207 | representative of the upper half of the distribution |
| `val_003167_typical_near_mean_psnr28.01.png` | typical | 28.01 | 0.8457 | sits at the reported 28.04 dB mean |
| `val_001682_typical_near_mean_psnr28.01.png` | typical | 28.01 | 0.8878 | second example at the mean, different content |
| `val_002041_weak_worst_in_split_psnr17.13.png` | weakest in the 400-pair split | 17.13 | 0.4499 | absolute worst-scoring validation pair |
| `val_000900_weak_lowest_ssim_in_worst8_psnr18.42.png` | weak, lowest SSIM among the 8 worst | 18.42 | 0.3195 | worst-8 group's minimum SSIM; a distinct failure mode (fine broadband texture) from the 002041/002042/002043 cluster |

**Documented failure case** (`failurecase_D5_*.png`, 1 file) — `docs/decisions.md` D5
identified `000984.npy` as the honest worst case by direct measurement (80.5% of GT spectral
energy lies above the LR Nyquist limit, i.e. provably unrecoverable by any model), and found
it to be **broadband noise-like texture, not the periodic aliasing grating SPEC §5.4
predicted** — see `results/eda/aliasing_failure_case.png` for the original spectral-energy
figure. `000984.npy` is **not a member of `configs/split_val.txt`** (it was identified by a
separate full-corpus screen, not the held-out split), so its PSNR/SSIM (10.52 dB / 0.2587) is
an **illustrative measurement only** — it is not part of, and does not change, the reported
28.0394 dB validation mean. The restoration was produced by the script issuing a live
`inference.py --require_weights` subprocess call against `weights/best.pt` (the real
production entry point, not a re-implemented forward pass) on a scratch single-file
input/output directory, so nothing about the actual scored split was touched.

**Final-test panels** (`finaltest_*.png`, 5 files) — two images side by side:
`NoisyLR | Restored`, titled in red, explicitly stating **NO GROUND TRUTH** and that no
PSNR/SSIM/LPIPS is computed or claimed. Source: the actual 400-file archive already generated
for submission (`results/restored_test_outputs/`, sha256
`a33a9a5a129bb006eccb5cf3367abad3456c63d96c1e7bb26e76800e7e375f98`; the local copy used to
render these panels was `/tmp/semicon_final_outputs_28db/`, produced by the exact
`inference.py --require_weights` run documented in `results/restored_test_outputs/README.md`
and `results/runtime_report.md`). Inputs are the released `test_NoisyLR` files (locally
`/Users/shanmukhsai/Downloads/NoisyLR`). Files chosen spread across the 400-file index
(`000000`, `000099`, `000199`, `000299`, `000399`) for coverage, not cherry-picked for
appearance.

## Files

```
manifest.json                  machine-written record of every file/PSNR/SSIM used above
val_*.png                      6 validation panels (NoisyLR | Restored | GT | error map)
failurecase_D5_*.png           1 out-of-split documented failure case (D5), same 4-panel layout
finaltest_*.png                5 final-test panels (NoisyLR | Restored only, no GT)
```

## Do not

- Do not read a final-test panel as carrying a quality score — there is no ground truth for
  the released test set, full stop (`docs/decisions.md`, README "Result summary").
- Do not fold the D5 case's 10.52 dB into the reported 28.0394 dB mean — they are computed
  over different, non-overlapping sets of files (400-pair `configs/split_val.txt` vs. one
  out-of-split file).

## Reproduction

```bash
.venv-mac/bin/python scripts/make_qualitative_examples.py \
  --data_root /Users/shanmukhsai/Downloads \
  --final_test_pred_dir /tmp/semicon_final_outputs_28db
```

`--data_root` must contain `train/GT` and `train/NoisyLR` (the val split lives inside
`train/`, selected by `configs/split_val.txt`) and, unless `--final_test_lr_dir` is given,
`<data_root>/NoisyLR` for the released test inputs. `--final_test_pred_dir` must already
contain the 400 generated final-test restorations (regenerate with the command in
`results/restored_test_outputs/README.md` if needed). Pass `--skip_d5` to skip the extra
`inference.py` subprocess call for the D5 panel.

The script loads `skimage.metrics.peak_signal_noise_ratio` / `structural_similarity` with the
settings pinned in `src/metrics.py`. It is **not** part of `scripts/verify_all.py` and does
not affect any V-check.
